<Page
    x:Class="scarDesktop.Views.StudyListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:scarDesktop.ViewModels"
    xmlns:models="using:scarDesktop.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewmodels:StudyListViewModel />
    </Page.DataContext>

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,0,0,20">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,12">
                    <Button x:ConnectionId='11' Content="대시보드"                              />
                </StackPanel>
                <TextBlock Text="스터디 목록" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock Text="관심있는 스터디를 찾아보세요" Style="{StaticResource BodyTextBlockStyle}" Foreground="Gray"/>
            </StackPanel>
            <Button x:ConnectionId='10' Content="스터디 만들기" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20,0,0,0"                                />
        </StackPanel>

        <ListView x:ConnectionId='2' Grid.Row="1" ItemsSource="{Binding Studies}" SelectionMode="Single"                                             >
            <ListView.ItemTemplate>
                <DataTemplate                          >
                    <Border x:ConnectionId='4' Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" 
                            CornerRadius="8" 
                            Padding="16" 
                            Margin="0,0,0,12">
                        <StackPanel Spacing="8">
                            <TextBlock x:ConnectionId='5'                       FontWeight="Bold" FontSize="18"/>
                            <TextBlock x:ConnectionId='6'                         TextTrimming="CharacterEllipsis" MaxLines="2" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Orientation="Horizontal" Spacing="4" Margin="0,8,0,0">
                                <TextBlock Text="리더:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock x:ConnectionId='7'                                 FontWeight="SemiBold"/>
                                <TextBlock Text="|" Margin="8,0,8,0" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="인원:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock x:ConnectionId='8'                                   />
                                <TextBlock Text="/"/>
                                <TextBlock x:ConnectionId='9'                          />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ProgressRing Grid.Row="1" IsActive="{Binding IsLoading}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>

