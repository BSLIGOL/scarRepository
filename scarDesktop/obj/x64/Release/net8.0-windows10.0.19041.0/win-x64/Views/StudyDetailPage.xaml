<Page
    x:Class="scarDesktop.Views.StudyDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:scarDesktop.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewmodels:StudyDetailViewModel />
    </Page.DataContext>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,12">
                <Button x:ConnectionId='3' Content="목록으로"                         />
                <Button x:ConnectionId='4' Content="대시보드"                              />
            </StackPanel>
            <TextBlock Text="{Binding Study.Title}" Style="{StaticResource TitleTextBlockStyle}" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding Study.Content}" Style="{StaticResource BodyTextBlockStyle}" Margin="0,8,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="24">
                <!-- Info Section -->
                <Grid ColumnSpacing="24" RowSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="리더:" FontWeight="Bold" Grid.Row="0" Grid.Column="0"/>
                    <TextBlock Text="{Binding Study.CreatorNickName}" Grid.Row="0" Grid.Column="1"/>

                    <TextBlock Text="인원:" FontWeight="Bold" Grid.Row="1" Grid.Column="0"/>
                    <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1" Spacing="4">
                        <TextBlock Text="{Binding Study.CurrentMemberCount}"/>
                        <TextBlock Text="/"/>
                        <TextBlock Text="{Binding Study.MaxMember}"/>
                        <TextBlock Text="명"/>
                    </StackPanel>
                </Grid>

                <!-- Members Section -->
                <StackPanel>
                    <TextBlock Text="스터디 멤버" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                    <ItemsControl ItemsSource="{Binding Study.Members}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="4" Padding="8,4" Margin="0,0,8,8" HorizontalAlignment="Left">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{Binding NickName}"/>
                                        <TextBlock Text="(리더)" Visibility="{Binding Role, Converter={StaticResource RoleToVisibilityConverter}}" Foreground="Orange"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="8"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button Content="스터디 참여 신청" Command="{Binding JoinCommand}" Visibility="{Binding CanJoin, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <!-- More buttons for Leave, Delete, etc. to be implemented -->
                </StackPanel>
                
                <!-- Loading/Error -->
                <ProgressRing IsActive="{Binding IsLoading}" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>

