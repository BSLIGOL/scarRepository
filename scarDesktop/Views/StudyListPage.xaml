<Page
    x:Class="scarDesktop.Views.StudyListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:scarDesktop.ViewModels"
    xmlns:models="using:scarDesktop.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewmodels:StudyListViewModel />
    </Page.DataContext>

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,0,0,20">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,12">
                    <Button Content="?€?œë³´?? Click="DashboardButton_Click"/>
                </StackPanel>
                <TextBlock Text="?¤í„°??ëª©ë¡" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock Text="ê´€?¬ìžˆ???¤í„°?”ë? ì°¾ì•„ë³´ì„¸?? Style="{StaticResource BodyTextBlockStyle}" Foreground="Gray"/>
            </StackPanel>
            <Button Content="?¤í„°??ë§Œë“¤ê¸? HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20,0,0,0" Click="CreateStudyButton_Click"/>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding Studies}" SelectionMode="Single" SelectionChanged="ListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Study">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" 
                            CornerRadius="8" 
                            Padding="16" 
                            Margin="0,0,0,12">
                        <StackPanel Spacing="8">
                            <TextBlock Text="{x:Bind Title}" FontWeight="Bold" FontSize="18"/>
                            <TextBlock Text="{x:Bind Content}" TextTrimming="CharacterEllipsis" MaxLines="2" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Orientation="Horizontal" Spacing="4" Margin="0,8,0,0">
                                <TextBlock Text="ë¦¬ë”:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind CreatorNickName}" FontWeight="SemiBold"/>
                                <TextBlock Text="|" Margin="8,0,8,0" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="?¸ì›:" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind CurrentMemberCount}"/>
                                <TextBlock Text="/"/>
                                <TextBlock Text="{x:Bind MaxMember}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ProgressRing Grid.Row="1" IsActive="{Binding IsLoading}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>
