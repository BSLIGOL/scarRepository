<Page
    x:Class="scarDesktop.Views.SignupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:scarDesktop.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewmodels:SignupViewModel />
    </Page.DataContext>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Spacing="10">
            <TextBlock Text="?Œì›ê°€?? FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBox Header="?´ë©”?? Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="example@email.com"/>
            <TextBox Header="?‰ë„¤?? Text="{Binding NickName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="?‰ë„¤??/>
            <PasswordBox Header="ë¹„ë?ë²ˆí˜¸" PasswordChanged="PasswordBox_PasswordChanged" PlaceholderText="?¢â€¢â€¢â€¢â€¢â€¢â€¢â€?/>
            <PasswordBox Header="ë¹„ë?ë²ˆí˜¸ ?•ì¸" PasswordChanged="ConfirmPasswordBox_PasswordChanged" PlaceholderText="?¢â€¢â€¢â€¢â€¢â€¢â€¢â€?/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" />

            <Button Command="{Binding SignupCommand}" Content="?Œì›ê°€?? HorizontalAlignment="Stretch" IsEnabled="{Binding IsLoading, Converter={StaticResource BoolNegationConverter}}"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <TextBlock Text="?´ë? ê³„ì •???ˆìœ¼? ê??? " Foreground="Gray"/>
                <HyperlinkButton Content="ë¡œê·¸?? Click="LoginButton_Click" Padding="0"/>
            </StackPanel>
            
            <ProgressRing IsActive="{Binding IsLoading}" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Page>
