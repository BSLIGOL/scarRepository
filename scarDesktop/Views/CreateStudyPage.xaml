<Page
    x:Class="scarDesktop.Views.CreateStudyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:scarDesktop.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewmodels:CreateStudyViewModel />
    </Page.DataContext>

    <Grid Padding="24">
        <StackPanel MaxWidth="600" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
            <TextBlock Text="?¤í„°??ë§Œë“¤ê¸? Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center" Margin="0,0,0,16"/>

            <TextBox Header="?¤í„°???´ë¦„" Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="?¤í„°???´ë¦„???…ë ¥?˜ì„¸??/>
            
            <TextBox Header="?¤í„°???Œê°œ" Text="{Binding Content, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="?¤í„°?”ì— ?€???¤ëª…???…ë ¥?˜ì„¸?? Height="150" AcceptsReturn="True" TextWrapping="Wrap"/>
            
            <NumberBox Header="ìµœë? ?¸ì›" Value="{Binding MaxMember, Mode=TwoWay}" Minimum="2" Maximum="100" SpinButtonPlacementMode="Inline"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="16" Margin="0,16,0,0">
                <Button Content="ì·¨ì†Œ" Click="CancelButton_Click" Width="100"/>
                <Button Content="ë§Œë“¤ê¸? Command="{Binding CreateStudyCommand}" Style="{StaticResource AccentButtonStyle}" Width="100" IsEnabled="{Binding IsLoading, Converter={StaticResource BoolNegationConverter}}"/>
            </StackPanel>

            <ProgressRing IsActive="{Binding IsLoading}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
