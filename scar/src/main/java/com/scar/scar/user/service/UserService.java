package com.scar.scar.user.service;

import com.scar.scar.user.domain.User;
import com.scar.scar.user.domain.GlobalRole;
import com.scar.scar.user.dto.UserRequestDto;
import com.scar.scar.user.repository.UserRepository;
import lombok.RequiredArgsConstructor;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service
@RequiredArgsConstructor
public class UserService {

    private final UserRepository userRepository;
    private final PasswordEncoder passwordEncoder;

    public User join(UserRequestDto dto) {

        if (userRepository.existsByEmail(dto.getEmail())) {
            throw new IllegalArgumentException("???? ???????嶺?????????????????롮쾸?椰??????????怨몄）.");
        }

        if (userRepository.existsByNickName(dto.getNickName())) {
            throw new IllegalArgumentException("???? ???????嶺??????????????????????꾩룆梨띰쭕??壤굿??щ첋????????怨몄）.");
        }

        User user = User.builder()
                .email(dto.getEmail())
                .password(passwordEncoder.encode(dto.getPassword()))
                .nickName(dto.getNickName())
                .globalRole(GlobalRole.USER)
                .build();

        return userRepository.save(user);
    }

    /**
     * ???????耀붾굝??????????????怨뚮뼺?됰뗀???(???????????
     */
    @Transactional
    public User updateProfile(Long userId, String newNickName) {
        User user = userRepository.findById(userId)
                .orElseThrow(() -> new IllegalArgumentException("?????? ??遺얘턁????????????????繹먮굞??????????怨몄）."));

        // ????????뉖???????? ???? ???????嶺??????????????????????꾩룆梨띰쭕??力?肉???怨뚮뼺?源낆┸????濚밸Ŧ??? ??耀붾굝??????????
        if (!user.getNickName().equals(newNickName) && userRepository.existsByNickName(newNickName)) {
            throw new IllegalArgumentException("???? ???????嶺??????????????????????꾩룆梨띰쭕??壤굿??щ첋????????怨몄）.");
        }

        user.updateNickName(newNickName);
        return userRepository.save(user);
    }

    /**
     * ????????????????怨뺤른????
     */
    @Transactional
    public void changePassword(Long userId, String currentPassword, String newPassword) {
        User user = userRepository.findById(userId)
                .orElseThrow(() -> new IllegalArgumentException("?????? ??遺얘턁????????????????繹먮굞??????????怨몄）."));

        // ??????꾩룆梨띰쭕???????????????耀붾굝??????????
        if (!passwordEncoder.matches(currentPassword, user.getPassword())) {
            throw new IllegalArgumentException("??????꾩룆梨띰쭕?????????????????? ??????롮쾸?椰????깅젿癲??? ?????????????怨몄）.");
        }

        // ?????????????????關?쒎첎?嫄???
        user.updatePassword(passwordEncoder.encode(newPassword));
        userRepository.save(user);
    }

    /**
     * ??????????????뽰맽
     */
    @Transactional
    public void deleteAccount(Long userId) {
        User user = userRepository.findById(userId)
                .orElseThrow(() -> new IllegalArgumentException("?????? ??遺얘턁????????????????繹먮굞??????????怨몄）."));

        // ??????꾩룆梨띰쭕???????????????????곌떽釉붾????????傭?
        userRepository.delete(user);
    }
}

